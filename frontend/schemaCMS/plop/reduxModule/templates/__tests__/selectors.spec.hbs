import { expect } from 'chai';
import Immutable from 'seamless-immutable';

import { select{{ pascalCase name }}Domain } from '../{{ camelCase name }}.selectors';

describe('{{ pascalCase name }}: selectors', () => {
  const state = Immutable({
    {{ camelCase name }}: {},
  });

  describe('select{{ pascalCase name }}Domain', () => {
    it('should select a domain', () => {
      expect(select{{ pascalCase name }}Domain(state)).to.equal(state.{{ camelCase name }});
    });
  });
});
