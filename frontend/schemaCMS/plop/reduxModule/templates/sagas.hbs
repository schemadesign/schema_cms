import { all, put, takeLatest } from 'redux-saga/effects';
import reportError from '../../shared/utils/reportError';

import { {{ pascalCase name }}Routines } from './{{ camelCase name }}.redux';

export function* watch{{ pascalCase name }}() {
  yield all([]);
}
