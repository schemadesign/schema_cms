import React from 'react';
import PropTypes from 'prop-types';

import { Container } from './blockElement.styles';

export const BlockElement = () => {
  return <Container>BlockElement component</Container>;
};

BlockElement.propTypes = {};
