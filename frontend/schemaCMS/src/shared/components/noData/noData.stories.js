import React from 'react';
import { storiesOf } from '@storybook/react';

import { NoData } from './noDadta.component';

storiesOf('Shared Components|NoData', module).add('Default', () => <NoData />);
