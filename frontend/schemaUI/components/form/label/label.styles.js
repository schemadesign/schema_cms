import { primary } from '../../../utils/theme';

export const defaultStyles = {
  border: 'none',
  outline: 'none',
  color: primary.label,
  fontSize: '14px',
  borderTop: `1px solid ${primary.divider}`,
  paddingTop: '8px',
  display: 'block',
  width: '100%',
};
