import { light } from '../../../utils/theme';

export const defaultStyles = {
  border: 'none',
  outline: 'none',
  color: light.label,
  fontSize: '14px',
  borderTop: `1px solid ${light.divider}`,
  paddingTop: '8px',
  display: 'block',
  width: '100%',
};
