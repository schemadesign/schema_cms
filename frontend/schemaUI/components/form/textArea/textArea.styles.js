import { light } from '../../../utils/theme';

export const defaultStyles = {
  border: 'none',
  outline: 'none',
  color: light.text,
  fontSize: '18px',
  lineHeight: '24px',
  resize: 'none',
  padding: '0',
};

export const shadowStyles = {
  visibility: 'hidden',
  position: 'absolute',
  overflow: 'hidden',
  height: 0,
  top: 0,
  left: 0,
};
