service: lambda-worker

provider:
  name: aws
  runtime: python3.7
  stage: ${opt:stage, 'dev'}


plugins:
  - serverless-python-requirements
  - serverless-localstack
  - serverless-package-external
custom:
  pythonRequirements:
    dockerizePip: non-linux
    slim: true
  localstack:
    stages:
      # list of stages for which the plugin should be enabled
      - local
    # optional - LocalStack host to connect to
    host: http://localstack
#    host: http://localhost
    lambda:
      mountCode: False
  packageExternal:
    external:
      - '../common'


package:
  exclude:
    - .git/**
    - node_modules/**

functions:
  main:
    handler: handler.main
    events:
      - sqs: arn:aws:sqs:us-east-1:000000000000:worker
    environment:
      DB_SECRET_ARN: arn:aws:secretsmanager:us-east-1:1234567890:secret:dbSecret
      AWS_STORAGE_BUCKET_NAME: schemacms
      LAMBDA_AUTH_TOKEN: 97090e73a80c4692bbbf7dedc17c59e0
